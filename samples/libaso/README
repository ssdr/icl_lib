c和c++函数调用互通问题:
1、编译 
1）引入头文件
2）在c/c++源文件开始处加入函数声明(与上面原理是一样的）
注意extern "C" 这个特殊符号只能出现在g++操作的文件中

2、链接
1) 链接为静态、动态库 (不用考虑c/c++之间的兼容问题）
2) 链接为二进制可执行文件 
   		c++调用c函数, 需要在c头文件或者c的函数声明中，加入extern "C" {}
		c函数调用c++， 不能使用头文件(gcc 不支持extern "C")，而要在被调用文件中，使用函数声明
	同时加上使用 extern "C"，再在调用文件中，声明此函数，即可.

